diff --git a/lib/src/AccessLogger.cc b/lib/src/AccessLogger.cc
index 7260976..0442a21 100644
--- a/lib/src/AccessLogger.cc
+++ b/lib/src/AccessLogger.cc
@@ -180,7 +180,11 @@ void AccessLogger::initAndStart(const Json::Value &config)
                     break;
                 }
                 // 2. check if we have rights to create files in the folder
+#if defined(__MINGW32__)
+                if (_waccess(fsLogPath.native().c_str(), W_OK) != 0)
+#else
                 if (os_access(fsLogPath.native().c_str(), W_OK) != 0)
+#endif
                 {
                     LOG_ERROR << "cannot create files in log folder";
                     break;
diff --git a/lib/src/HttpAppFrameworkImpl.cc b/lib/src/HttpAppFrameworkImpl.cc
index 42309ee..3b01138 100644
--- a/lib/src/HttpAppFrameworkImpl.cc
+++ b/lib/src/HttpAppFrameworkImpl.cc
@@ -1166,7 +1166,11 @@ HttpAppFramework &HttpAppFrameworkImpl::setupFileLogger()
                     abort();
                 }
                 // 2. check if we have rights to create files in the folder
+#if defined(__MINGW32__)
+                if (_waccess(fsLogPath.native().c_str(), W_OK) != 0)
+#else
                 if (os_access(fsLogPath.native().c_str(), W_OK) != 0)
+#endif
                 {
                     LOG_ERROR << "cannot create files in log folder";
                     abort();
diff --git a/lib/src/Utilities.cc b/lib/src/Utilities.cc
index c6601f6..ea22e5f 100644
--- a/lib/src/Utilities.cc
+++ b/lib/src/Utilities.cc
@@ -1290,10 +1290,11 @@ std::string secureRandomString(size_t size)
 
     // batch up to 32 bytes of random data for efficiency. Calling
     // secureRandomBytes can be expensive.
-    auto randByte = []() {
-        thread_local trantor::utils::Hash256 hash;
-        thread_local size_t i = 0;
-        if (i == 0)
+    trantor::utils::Hash256 hash;
+    for (size_t i = 0; i < size; ++i)
+    {
+        auto j = i % sizeof(hash);
+        if (j == 0)
         {
             bool ok = trantor::utils::secureRandomBytes(&hash, sizeof(hash));
             if (!ok)
@@ -1301,13 +1302,8 @@ std::string secureRandomString(size_t size)
                     "Failed to generate random bytes for secureRandomString");
         }
         unsigned char *hashBytes = reinterpret_cast<unsigned char *>(&hash);
-        auto ret = hashBytes[i];
-        i = (i + 1) % sizeof(hash);
-        return ret;
-    };
-
-    for (size_t i = 0; i < size; ++i)
-        ret[i] = chars[randByte() % 64];
+        ret[i] = chars[hashBytes[j] % 64];
+    }
     return ret;
 }
 
@@ -1318,7 +1314,7 @@ const size_t fixedRandomNumber = []() {
     utils::secureRandomBytes(&res, sizeof(res));
     return res;
 }();
-}
+}  // namespace internal
 
 }  // namespace utils
 }  // namespace drogon
